import { Radio } from '@mui/material'
import React from 'react'

const AddressCard = ({data}:any) => {
    const handleChange = (event:any)=>{
        console.log(event.target.checked)
    }

  return (
   <></>
  )
}

export default AddressCard